# Docker ビルドコンテキストから除外する（イメージに不要 / キャッシュを汚さない）
# 設計根拠: docs/adr/0004-docker-test-environment.md

# Git / Claude Code 作業領域
.git
.claude

# Python の仮想環境・キャッシュ（イメージ内で作り直す）
.venv
**/__pycache__
**/*.py[cod]
**/*.egg-info
.pytest_cache
.mypy_cache
.ruff_cache
build
dist

# Docker 定義自体（イメージ内に取り込む必要はない）
docker
compose.yaml
.dockerignore

# OS / エディタ
.DS_Store
