[project]
name = "tools"
version = "0.0.0"
description = "ユーティリティスクリプト集（個人向け汎用ツール）"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tools"]
